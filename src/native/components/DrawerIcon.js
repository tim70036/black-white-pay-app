import React from 'react';
import { Icon } from 'native-base';

const DrawerIcon = () => (
  <Icon ios="ios-menu" android="md-menu" style={{ fontSize: 40, color: 'red' }} />
);

export default DrawerIcon;
